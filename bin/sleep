#!/bin/bash

# Lock the screen
i3lock -i ~/backgrounds/nice-blue-background.png

# Save session (Optional, if you want to save your session state)
# i3-save-session

# Put the system to sleep
systemctl suspend
